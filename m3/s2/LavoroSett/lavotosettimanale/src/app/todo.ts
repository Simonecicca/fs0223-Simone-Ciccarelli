import { compileDirectiveFromMetadata } from "@angular/compiler"

export interface Todo {

  id?: number,
  title: string,
  completed: boolean
}


// Ho provato, guardando il possibile ma anche fixando gli errori non funziona, forse cercando qua la e spulciando anche il tuo codice
// ho mischiato cose sbagliate, non mi interessa il voto in se , mai interessato, però se nel commento può indicarmi l'errore/gli errori principali
// ne sarei grato, grazie!
